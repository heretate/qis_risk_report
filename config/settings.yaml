bloomberg:
  host: "localhost"
  port: 8194
  timeout_seconds: 30

reports:
  output_dir: "output"
  template_dir: "src/qis_risk_report/reports/templates"
  default_format: "excel"   # excel | pdf | html

data:
  input_dir: "data/input"
  date_format: "%Y-%m-%d"
